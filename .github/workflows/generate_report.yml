name: Generate Issue Management Report

on:
  # Roda toda segunda-feira às 8h (horário de Brasília = 11h UTC)
  schedule:
    - cron: '0 11 * * 1'

  # Permite rodar manualmente pela interface do GitHub
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate dashboard
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_WAREHOUSE_ID: ${{ secrets.DATABRICKS_WAREHOUSE_ID }}
        run: python scripts/generate_report.py

      - name: Publish to Confluence
        env:
          CONFLUENCE_URL:     ${{ secrets.CONFLUENCE_URL }}
          CONFLUENCE_EMAIL:   ${{ secrets.CONFLUENCE_EMAIL }}
          CONFLUENCE_TOKEN:   ${{ secrets.CONFLUENCE_TOKEN }}
          CONFLUENCE_PAGE_ID: ${{ secrets.CONFLUENCE_PAGE_ID }}
        run: |
          python3 - <<'EOF'
          import os, urllib.request, urllib.parse, base64, json

          url     = os.environ['CONFLUENCE_URL']
          email   = os.environ['CONFLUENCE_EMAIL']
          token   = os.environ['CONFLUENCE_TOKEN']
          page_id = os.environ['CONFLUENCE_PAGE_ID']

          creds = base64.b64encode(f"{email}:{token}".encode()).decode()
          headers_base = {
              'Authorization': f'Basic {creds}',
              'X-Atlassian-Token': 'no-check',
          }

          # 1. Verifica se já existe o anexo index.html
          req = urllib.request.Request(
              f"{url}/wiki/rest/api/content/{page_id}/child/attachment?filename=index.html",
              headers={**headers_base, 'Accept': 'application/json'}
          )
          with urllib.request.urlopen(req) as r:
              existing = json.loads(r.read())

          with open('docs/index.html', 'rb') as f:
              file_data = f.read()

          results = existing.get('results', [])
          if results:
              # Atualiza anexo existente
              att_id = results[0]['id']
              boundary = 'boundary123'
              body = (
                  f'--{boundary}\r\n'
                  f'Content-Disposition: form-data; name="file"; filename="index.html"\r\n'
                  f'Content-Type: text/html\r\n\r\n'
              ).encode() + file_data + f'\r\n--{boundary}--\r\n'.encode()

              req = urllib.request.Request(
                  f"{url}/wiki/rest/api/content/{page_id}/child/attachment/{att_id}/data",
                  data=body, method='POST',
                  headers={**headers_base, 'Content-Type': f'multipart/form-data; boundary={boundary}'}
              )
              print(f"Atualizando anexo {att_id}...")
          else:
              # Cria novo anexo
              boundary = 'boundary123'
              body = (
                  f'--{boundary}\r\n'
                  f'Content-Disposition: form-data; name="file"; filename="index.html"\r\n'
                  f'Content-Type: text/html\r\n\r\n'
              ).encode() + file_data + f'\r\n--{boundary}--\r\n'.encode()

              req = urllib.request.Request(
                  f"{url}/wiki/rest/api/content/{page_id}/child/attachment",
                  data=body, method='POST',
                  headers={**headers_base, 'Content-Type': f'multipart/form-data; boundary={boundary}'}
              )
              print("Criando anexo...")

          with urllib.request.urlopen(req) as r:
              resp = json.loads(r.read())
          results = resp.get('results', [resp])
          att = results[0] if results else resp
          print(f"✓ Publicado no Confluence: {url}/wiki/spaces/lending/pages/{page_id}")
          print(f"  Anexo: {att.get('title', 'index.html')}")
          EOF

      - name: Commit dashboard to repo
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git diff --cached --quiet && echo "Sem mudanças." || \
            git commit -m "chore: update dashboard $(date +'%Y-%m-%d')" && git push
